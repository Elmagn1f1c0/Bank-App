@using Books.Models;
@using Microsoft.AspNetCore.Identity;
@inject UserManager<ApplicationUser> UserManager;

@if (!User.Identity.IsAuthenticated)
{
   @*  <ul class="navbar-nav flex-grow-1 d-flex justify-content-end">

        <li class="nav-item dropdown parentcont">
            <a class="nav-link dropdown-toggle" id="btnDrop" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <i class="bi bi-person"></i> User Account
            </a>
            <div id="hide-drop-down">
                <a class="dropdown-item" asp-controller="Auth" asp-action="Register"><i class="bi bi-person-circle"></i> Register</a>
                <a class="dropdown-item" asp-controller="Auth" asp-action="Login"><i class="bi bi-box-arrow-right"></i> Login</a>

            </div>
        </li>

    </ul> *@
} else
{
    <button class="btn btn-outline-success" type="button" id="profile-btn"
            aria-expanded="false">
        <i class="bi bi-person-badge"></i> Hello @UserManager.GetUserName(User)
    </button>
<form asp-controller="Auth" asp-action="Logout">
    <button class="btn btn-outline-danger my-2 my-sm-0 d-flex justify-content-end">
        <i class="bi bi-box-arrow-in-right"></i> Log out
    </button>
</form>
}